name: Deploy to GitHub Pages (Disabled)
# Cloudflare Pages now handles deploys automatically from main branch
# This workflow is disabled to prevent gh-pages branch conflicts

on:
  workflow_dispatch: # manual only

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy handled by Cloudflare Pages"
